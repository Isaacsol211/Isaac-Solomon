node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Impeccable engine binary — 12 MB, macOS-only, re-fetched by `npx impeccable install`.
# The hook in .claude/settings.local.json is guarded and no-ops when it is absent.
.claude/skills/impeccable/scripts/bin/

# Playwright
/test-results
